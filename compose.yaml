services:
  solar:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/solar.db
      SOLAR_ATTACHMENTS_DIR: /data/attachments
      SOLAR_PI_AGENT_DIR: /data/pi-agent
    volumes:
      - ./data:/data
